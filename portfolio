<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jashwanth KL - DevOps Engineer</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #333;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header Section */
        .header {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 40px;
            margin-bottom: 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.2);
            animation: slideInDown 1s ease-out;
        }

        .header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            animation: shimmer 3s infinite;
        }

        .profile-img {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            background: linear-gradient(45deg, #ff6b6b, #4ecdc4);
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 48px;
            color: white;
            font-weight: bold;
            animation: pulse 2s infinite;
            position: relative;
            z-index: 1;
        }

        .header h1 {
            color: white;
            font-size: 2.5em;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
            position: relative;
            z-index: 1;
        }

        .header p {
            color: rgba(255, 255, 255, 0.9);
            font-size: 1.1em;
            margin-bottom: 20px;
            position: relative;
            z-index: 1;
        }

        .contact-info {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
            position: relative;
            z-index: 1;
        }

        .contact-item {
            background: rgba(255, 255, 255, 0.2);
            padding: 8px 16px;
            border-radius: 25px;
            color: white;
            text-decoration: none;
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.3);
        }

        .contact-item:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        /* Section Styling */
        .section {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            animation: slideInUp 0.8s ease-out;
        }

        .section h2 {
            color: #4a5568;
            font-size: 2em;
            margin-bottom: 20px;
            position: relative;
            display: inline-block;
        }

        .section h2::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 100%;
            height: 3px;
            background: linear-gradient(90deg, #ff6b6b, #4ecdc4);
            border-radius: 2px;
        }

        /* Skills Grid */
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .skill-category {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            border-radius: 15px;
            padding: 20px;
            color: white;
            transition: transform 0.3s ease;
        }

        .skill-category:hover {
            transform: translateY(-5px) scale(1.02);
        }

        .skill-category h3 {
            margin-bottom: 15px;
            font-size: 1.3em;
        }

        .skill-item {
            background: rgba(255, 255, 255, 0.2);
            padding: 8px 12px;
            border-radius: 20px;
            margin: 5px;
            display: inline-block;
            font-size: 0.9em;
            transition: all 0.3s ease;
        }

        .skill-item:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.05);
        }

        /* AWS Services */
        .aws-services {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .aws-service {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 15px;
            border-radius: 10px;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .aws-service:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        /* Project Section */
        .project {
            background: linear-gradient(135deg, #4ecdc4 0%, #44a08d 100%);
            border-radius: 15px;
            padding: 25px;
            color: white;
            margin-top: 20px;
        }

        .project h3 {
            margin-bottom: 15px;
            font-size: 1.4em;
        }

        .tech-stack {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin: 15px 0;
        }

        .tech-item {
            background: rgba(255, 255, 255, 0.2);
            padding: 6px 12px;
            border-radius: 15px;
            font-size: 0.9em;
        }

        .responsibilities {
            margin-top: 15px;
        }

        .responsibilities li {
            margin: 8px 0;
            padding-left: 10px;
            position: relative;
        }

        .responsibilities li::before {
            content: '▶';
            position: absolute;
            left: -10px;
            color: #fff;
        }

        /* Education Timeline */
        .education-timeline {
            position: relative;
            padding: 20px 0;
        }

        .education-timeline::before {
            content: '';
            position: absolute;
            left: 30px;
            top: 0;
            bottom: 0;
            width: 3px;
            background: linear-gradient(180deg, #ff6b6b, #4ecdc4);
            border-radius: 2px;
        }

        .education-item {
            position: relative;
            margin: 20px 0 20px 60px;
            background: rgba(255, 255, 255, 0.8);
            padding: 20px;
            border-radius: 10px;
            border-left: 4px solid #4ecdc4;
            transition: all 0.3s ease;
        }

        .education-item:hover {
            transform: translateX(10px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .education-item::before {
            content: '';
            position: absolute;
            left: -42px;
            top: 25px;
            width: 12px;
            height: 12px;
            background: #4ecdc4;
            border-radius: 50%;
            border: 3px solid white;
        }

        /* Strengths */
        .strengths-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .strength-item {
            background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
            padding: 20px;
            border-radius: 15px;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .strength-item:hover {
            transform: translateY(-5px) rotate(2deg);
            box-shadow: 0 10px 25px rgba(0,0,0,0.15);
        }

        /* Languages & Hobbies */
        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        .info-card {
            background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%);
            padding: 20px;
            border-radius: 15px;
            transition: all 0.3s ease;
        }

        .info-card:hover {
            transform: scale(1.02);
        }

        .info-card h3 {
            color: #4a5568;
            margin-bottom: 15px;
        }

        .info-item {
            background: rgba(255, 255, 255, 0.7);
            padding: 8px 15px;
            margin: 8px 0;
            border-radius: 20px;
            display: inline-block;
        }

        /* Personal Info */
        .personal-info {
            background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%);
            padding: 25px;
            border-radius: 15px;
            margin-top: 20px;
        }

        .personal-info h3 {
            color: #4a5568;
            margin-bottom: 20px;
            text-align: center;
        }

        .info-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 15px;
        }

        .info-detail {
            background: rgba(255, 255, 255, 0.8);
            padding: 15px;
            border-radius: 10px;
            text-align: center;
        }

        .info-detail strong {
            color: #4a5568;
            display: block;
            margin-bottom: 5px;
        }

        /* Animations */
        @keyframes slideInDown {
            from {
                opacity: 0;
                transform: translateY(-100px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        @keyframes shimmer {
            0% { transform: translateX(-100%); }
            100% { transform: translateX(100%); }
        }

        /* Floating Elements */
        .floating-shapes {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
        }

        .floating-shape {
            position: absolute;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            animation: float 6s ease-in-out infinite;
        }

        .floating-shape:nth-child(1) { width: 80px; height: 80px; top: 20%; left: 10%; animation-delay: 0s; }
        .floating-shape:nth-child(2) { width: 120px; height: 120px; top: 60%; left: 80%; animation-delay: 2s; }
        .floating-shape:nth-child(3) { width: 60px; height: 60px; top: 80%; left: 20%; animation-delay: 4s; }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }
            
            .header {
                padding: 30px 20px;
            }
            
            .header h1 {
                font-size: 2em;
            }
            
            .contact-info {
                flex-direction: column;
                align-items: center;
            }
            
            .skills-grid {
                grid-template-columns: 1fr;
            }
            
            .aws-services {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            }

            .info-details {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="floating-shapes">
        <div class="floating-shape"></div>
        <div class="floating-shape"></div>
        <div class="floating-shape"></div>
    </div>

    <div class="container">
        <!-- Header Section -->
        <header class="header">
            <div class="profile-img">JK</div>
            <h1>JASHWANTH KL</h1>
            <p>DevOps Engineer | Cloud Enthusiast | Problem Solver</p>
            <div class="contact-info">
                <a href="tel:+917975532351" class="contact-item">📞 +91 7975532351</a>
                <a href="mailto:jashwanthkl1113@gmail.com" class="contact-item">✉️ jashwanthkl1113@gmail.com</a>
                <a href="https://linkedin.com/in/jashwanth-kl-43439a2b7" class="contact-item" target="_blank">🔗 LinkedIn</a>
                <span class="contact-item">📍 Bangalore-560091</span>
            </div>
        </header>

        <!-- Career Objective -->
        <section class="section">
            <h2>🎯 Career Objective</h2>
            <p style="font-size: 1.1em; line-height: 1.6; color: #555;">
                To work in a challenging environment where I can show my skills, knowledge, and creativity to fulfill organizational needs and develop myself as the best asset for the company.
            </p>
        </section>

        <!-- Technical Skills -->
        <section class="section">
            <h2>💻 Technical Skills</h2>
            <div class="skills-grid">
                <div class="skill-category">
                    <h3>🔧 DevOps Tools</h3>
                    <div class="skill-item">Git</div>
                    <div class="skill-item">Jenkins</div>
                    <div class="skill-item">Maven</div>
                    <div class="skill-item">Ansible</div>
                    <div class="skill-item">Docker</div>
                    <div class="skill-item">Terraform</div>
                </div>
            </div>
        </section>

        <!-- AWS Services -->
        <section class="section">
            <h2>☁️ AWS Services</h2>
            <div class="aws-services">
                <div class="aws-service">EC2</div>
                <div class="aws-service">S3</div>
                <div class="aws-service">IAM</div>
                <div class="aws-service">CLI</div>
                <div class="aws-service">RDS</div>
                <div class="aws-service">Cross Region Replication</div>
                <div class="aws-service">ELB</div>
                <div class="aws-service">EBS</div>
                <div class="aws-service">VPC</div>
            </div>
        </section>

        <!-- Project -->
        <section class="section">
            <h2>🚀 Project</h2>
            <div class="project">
                <h3>Web Application Deployment Pipeline</h3>
                <p>Deployed source code to Tomcat web application server using modern DevOps practices and cloud infrastructure.</p>
                
                <div class="tech-stack">
                    <div class="tech-item">Git & GitHub</div>
                    <div class="tech-item">Maven</div>
                    <div class="tech-item">Jenkins</div>
                    <div class="tech-item">Apache Tomcat</div>
                    <div class="tech-item">Terraform</div>
                    <div class="tech-item">AWS</div>
                    <div class="tech-item">S3</div>
                </div>
                
                <div class="responsibilities">
                    <h4>Key Achievements:</h4>
                    <ul>
                        <li>Created infrastructure using Terraform via Jenkins pipeline</li>
                        <li>Set up Jenkins master/slave architecture for build and deployment</li>
                        <li>Deployed Tomcat server on the slave machine</li>
                        <li>Integrated Jenkins with Maven to build the WAR file</li>
                        <li>Uploaded artifacts to S3 for versioning</li>
                        <li>Used freestyle job in Jenkins for deployment</li>
                    </ul>
                </div>
            </div>
        </section>

        <!-- Education -->
        <section class="section">
            <h2>🎓 Education</h2>
            <div class="education-timeline">
                <div class="education-item">
                    <h3>B.E (Computer Science Engineering)</h3>
                    <p><strong>Sri Krishna Institute of Technology</strong></p>
                    <p>VTU University | 2025 | 7.7 CGPA</p>
                </div>
                <div class="education-item">
                    <h3>Diploma (Information Science Technology)</h3>
                    <p><strong>PVP Polytechnic</strong></p>
                    <p>DTE Board | 2022 | 82%</p>
                </div>
                <div class="education-item">
                    <h3>SSLC</h3>
                    <p><strong>SS Public High School</strong></p>
                    <p>State Board | 2019 | 80%</p>
                </div>
            </div>
        </section>

        <!-- Strengths -->
        <section class="section">
            <h2>💪 Strengths</h2>
            <div class="strengths-grid">
                <div class="strength-item">
                    <h3>🤝 Team Player</h3>
                    <p>Collaborative mindset with excellent teamwork skills</p>
                </div>
                <div class="strength-item">
                    <h3>🔄 Adaptable & Flexible</h3>
                    <p>Quick to adapt to new technologies and environments</p>
                </div>
                <div class="strength-item">
                    <h3>💬 Strong Communication</h3>
                    <p>Excellent verbal and written communication abilities</p>
                </div>
            </div>
        </section>

        <!-- Languages & Hobbies -->
        <section class="section">
            <h2>🌟 Additional Information</h2>
            <div class="info-grid">
                <div class="info-card">
                    <h3>🗣️ Languages</h3>
                    <div class="info-item">English (Speak, Write, Read)</div>
                    <div class="info-item">Kannada (Speak, Write, Read)</div>
                    <div class="info-item">Hindi (Write, Read)</div>
                </div>
                <div class="info-card">
                    <h3>🎯 Hobbies</h3>
                    <div class="info-item">🔬 Exploring New Technologies</div>
                    <div class="info-item">🎵 Singing</div>
                    <div class="info-item">🏏 Cricket</div>
                </div>
            </div>
        </section>

        <!-- Personal Information -->
        <section class="section">
            <div class="personal-info">
                <h3>👤 Personal Information</h3>
                <div class="info-details">
                    <div class="info-detail">
                        <strong>Father's Name:</strong>
                        Lokesh K
                    </div>
                    <div class="info-detail">
                        <strong>Date of Birth:</strong>
                        01/11/2003
                    </div>
                    <div class="info-detail">
                        <strong>Age:</strong>
                        22 years
                    </div>
                    <div class="info-detail">
                        <strong>Gender:</strong>
                        Male
                    </div>
                    <div class="info-detail">
                        <strong>Marital Status:</strong>
                        Single
                    </div>
                    <div class="info-detail">
                        <strong>Address:</strong>
                        #24, 15th Cross, Vigneshwara Nagar, Sunkadakatte, Bangalore - 560091
                    </div>
                </div>
            </div>
        </section>
    </div>

    <script>
        // Add smooth scrolling and interactive effects
        document.addEventListener('DOMContentLoaded', function() {
            // Animate sections on scroll
            const sections = document.querySelectorAll('.section');
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.animationDelay = '0.2s';
                        entry.target.style.animationPlayState = 'running';
                    }
                });
            }, { threshold: 0.1 });

            sections.forEach(section => {
                observer.observe(section);
            });

            // Add click effects to skill items and AWS services
            const clickableItems = document.querySelectorAll('.skill-item, .aws-service, .strength-item');
            clickableItems.forEach(item => {
                item.addEventListener('click', function() {
                    this.style.transform = 'scale(0.95)';
                    setTimeout(() => {
                        this.style.transform = '';
                    }, 150);
                });
            });

            // Add typing effect to the name
            const nameElement = document.querySelector('.header h1');
            const originalText = nameElement.textContent;
            nameElement.textContent = '';
            
            setTimeout(() => {
                let i = 0;
                const typeInterval = setInterval(() => {
                    nameElement.textContent += originalText[i];
                    i++;
                    if (i >= originalText.length) {
                        clearInterval(typeInterval);
                    }
                }, 100);
            }, 1000);
        });
    </script>
</body>
</html>
